# WARNING: This file is managed automatically and may be overwritten at any time.

name: Release

on:
  push:
    branches: [main, master]

jobs:
  Release:
    uses: schubergphilis-ep/github-terraform-workflows/.github/workflows/module-release.yaml@v2.0.0
    permissions:
      contents: write
      issues: write
      pull-requests: write
    secrets: inherit
